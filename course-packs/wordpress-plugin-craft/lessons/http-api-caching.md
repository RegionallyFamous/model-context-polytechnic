Plugins often call external APIs. Use WordPress HTTP APIs, set timeouts, validate responses, avoid leaking secrets, and cache expensive or rate-limited reads with transients or persistent cache strategies. Source: https://developer.wordpress.org/plugins/

LLMs tend to omit cache and failure paths. A useful course should punish that omission gently but consistently.
